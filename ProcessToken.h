#ifndef __PROCESSTOKEN_H__
#define __PROCESSTOKEN_H__


void ProcessToken(std::string word, int index) {
  //here we process the token

  if(index==5)
    std::cout << word << std::endl;
  return;
}

#endif
